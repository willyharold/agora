{% extends 'base.html.twig' %}

{% block body %}
    <h1>Symptome_maladie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ symptome_maladie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ symptome_maladie.nom }}</td>
            </tr>
            <tr>
                <th>Caracteristique</th>
                <td>{{ symptome_maladie.caracteristique }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('symptome_maladie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('symptome_maladie_edit', { 'id': symptome_maladie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
